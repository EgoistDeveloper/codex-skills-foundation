# Engineering Foundation plugin

The plugin provides portable Agent Skills. Custom agents are optional accelerators, not dependencies.

Start with `engineering-router` for a mixed or uncertain task. Directly invoke a domain skill when the route is obvious.

Core rule: one primary writer, fresh evidence before completion, and no implementation reopening without a material reason.

See the repository root README for installation, Cloud setup, compatibility, and validation.
