name: Apply process-level negative smoke isolation fix

on:
  push:
    branches:
      - fix/codex-negative-smoke-startup-isolation

permissions:
  contents: write

jobs:
  patch-and-validate:
    runs-on: ubuntu-latest
    steps:
      - name: Check out branch
        uses: actions/checkout@v4
        with:
          ref: fix/codex-negative-smoke-startup-isolation
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install validation dependencies
        run: python -m pip install --requirement requirements-dev.txt

      - name: Apply harness and test changes
        shell: bash
        run: |
          python - <<'PY'
          from pathlib import Path

          def replace_once(text: str, old: str, new: str, label: str) -> str:
              count = text.count(old)
              if count != 1:
                  raise SystemExit(f"{label}: expected one match, found {count}")
              return text.replace(old, new, 1)

          script_path = Path("scripts/run_codex_negative_smoke.py")
          text = script_path.read_text(encoding="utf-8")

          marker = "\n\ndef completed_verify_commands(turn: base.LiveTurn) -> list[base.CommandEvidence]:\n"
          helpers = r'''


def toml_bool(value: bool) -> str:
    return "true" if value else "false"


def plugin_table_override(plugin_states: dict[str, bool]) -> str:
    entries = ", ".join(
        f"{json.dumps(plugin_id, ensure_ascii=True)} = {{ enabled = {toml_bool(enabled)} }}"
        for plugin_id, enabled in sorted(plugin_states.items())
    )
    return f"plugins={{ {entries} }}"


def build_isolated_app_server_command(
    *,
    launchers: base.CodexLaunchers,
    installed_plugin_ids: list[str],
    plugins_enabled: bool,
    enabled_plugin_id: str | None,
) -> tuple[tuple[str, ...], list[str]]:
    plugin_ids = sorted(set(installed_plugin_ids))
    if plugins_enabled:
        if enabled_plugin_id is None:
            raise base.HarnessError("an enabled plugin id is required for candidate startup.")
        if enabled_plugin_id not in plugin_ids:
            raise base.HarnessError(
                f"startup plugin inventory did not contain {enabled_plugin_id!r}."
            )
    elif enabled_plugin_id is not None:
        raise base.HarnessError("baseline startup cannot select an enabled plugin id.")

    plugin_states = {
        plugin_id: plugins_enabled and plugin_id == enabled_plugin_id
        for plugin_id in plugin_ids
    }
    overrides = [
        f"features.plugins={toml_bool(plugins_enabled)}",
        "features.remote_plugin=false",
        "features.recommended_plugins=false",
        "features.plugin_sharing=false",
        "features.apps=false",
        "features.code_mode=false",
        "memories.generate_memories=false",
        "memories.use_memories=false",
        "memories.dedicated_tools=false",
    ]
    if plugin_states:
        overrides.append(plugin_table_override(plugin_states))

    command: list[str] = [*launchers.cli_prefix, "app-server"]
    for override in overrides:
        command.extend(("-c", override))
    command.extend(("--listen", "stdio://"))
    return tuple(command), overrides
'''
          text = replace_once(text, marker, helpers + marker, "insert startup isolation helpers")

          old = '''    config["features"]["plugins"] = True
    config["plugins"] = {
        plugin_id: {"enabled": plugin_id == base.PLUGIN_ID}
        for plugin_id in sorted(set(installed_plugin_ids) | {base.PLUGIN_ID})
    }
    return config, discovered_core, foreign_paths, foreign_plugin_ids
'''
          new = '''    config["features"]["plugins"] = True
    config["features"]["remote_plugin"] = False
    config["features"]["recommended_plugins"] = False
    config["features"]["plugin_sharing"] = False
    config["features"]["code_mode"] = False
    config["plugins"] = {
        plugin_id: {"enabled": plugin_id == base.PLUGIN_ID}
        for plugin_id in sorted(set(installed_plugin_ids) | {base.PLUGIN_ID})
    }
    config["memories"] = {
        "generate_memories": False,
        "use_memories": False,
        "dedicated_tools": False,
    }
    return config, discovered_core, foreign_paths, foreign_plugin_ids
'''
          text = replace_once(text, old, new, "strengthen candidate thread config")

          old = '''    disabled_skill_paths: list[str],
    disabled_plugin_ids: list[str],
    exposed_core_skills: dict[str, str],
) -> NegativeEvaluation:
'''
          new = '''    disabled_skill_paths: list[str],
    disabled_plugin_ids: list[str],
    startup_config_overrides: list[str],
    exposed_core_skills: dict[str, str],
) -> NegativeEvaluation:
'''
          text = replace_once(text, old, new, "extend evaluation signature")

          old = '''        "environment_findings": environment_findings,
        "disabled_plugin_ids": sorted(set(disabled_plugin_ids)),
        "token_usage": token_usage,
'''
          new = '''        "environment_findings": environment_findings,
        "disabled_plugin_ids": sorted(set(disabled_plugin_ids)),
        "startup_config_overrides": list(startup_config_overrides),
        "token_usage": token_usage,
'''
          text = replace_once(text, old, new, "record startup overrides")

          old = '''    service_tier: str | None,
    session_config: dict[str, Any],
) -> tuple[base.LiveTurn, Path]:
'''
          new = '''    service_tier: str | None,
    session_config: dict[str, Any],
    app_server_command: tuple[str, ...],
) -> tuple[base.LiveTurn, Path]:
'''
          text = replace_once(text, old, new, "extend live variant signature")

          old = '''    with base.AppServer(
        command=launchers.app_server_command,
        node_executable=launchers.node_executable,
'''
          new = '''    with base.AppServer(
        command=app_server_command,
        node_executable=launchers.node_executable,
'''
          text = replace_once(text, old, new, "use supplied app-server command")

          old = '''        "disabled_plugin_ids": artifact.get("disabled_plugin_ids", []),
        "observed_core_skill_reads": artifact.get("observed_core_skill_reads", []),
'''
          new = '''        "disabled_plugin_ids": artifact.get("disabled_plugin_ids", []),
        "startup_config_overrides": artifact.get("startup_config_overrides", []),
        "observed_core_skill_reads": artifact.get("observed_core_skill_reads", []),
'''
          text = replace_once(text, old, new, "include startup overrides in diagnostics")

          old = '''    if isinstance(candidate, dict):
        findings = candidate.get("environment_findings", [])
        if findings:
            print("  candidate-environment: " + "; ".join(str(item) for item in findings))
    print(f"  file   : {path}")
'''
          new = '''    if isinstance(candidate, dict):
        findings = candidate.get("environment_findings", [])
        if findings:
            print("  candidate-environment: " + "; ".join(str(item) for item in findings))
        disabled_plugins = candidate.get("disabled_plugin_ids", [])
        if disabled_plugins:
            print("  disabled-plugins: " + ", ".join(str(item) for item in disabled_plugins))
        startup_overrides = candidate.get("startup_config_overrides", [])
        if startup_overrides:
            print(f"  startup-overrides: {len(startup_overrides)}")
    print(f"  file   : {path}")
'''
          text = replace_once(text, old, new, "print richer diagnostics")

          old = '''            guard.snapshot_config(codex_home)
            guard.prepare_baseline()

            with base.AppServer(
                command=launchers.app_server_command,
'''
          new = '''            guard.snapshot_config(codex_home)
            guard.prepare_baseline()
            baseline_plugin_ids = installed_plugin_ids(launchers)
            (
                baseline_app_server_command,
                baseline_startup_overrides,
            ) = build_isolated_app_server_command(
                launchers=launchers,
                installed_plugin_ids=baseline_plugin_ids,
                plugins_enabled=False,
                enabled_plugin_id=None,
            )

            with base.AppServer(
                command=baseline_app_server_command,
'''
          text = replace_once(text, old, new, "build baseline startup isolation")

          old = '''            baseline_config = base.build_session_config(
                disabled_skill_paths=baseline_disabled_skills,
                mcp_server_names=mcp_names,
            )

            print("[1/2] Running isolated plugin-disabled tiny-edit baseline...")
'''
          new = '''            baseline_config = base.build_session_config(
                disabled_skill_paths=baseline_disabled_skills,
                mcp_server_names=mcp_names,
            )
            baseline_config["features"]["remote_plugin"] = False
            baseline_config["features"]["recommended_plugins"] = False
            baseline_config["features"]["plugin_sharing"] = False
            baseline_config["features"]["code_mode"] = False
            baseline_config["memories"] = {
                "generate_memories": False,
                "use_memories": False,
                "dedicated_tools": False,
            }

            print("[1/2] Running isolated plugin-disabled tiny-edit baseline...")
'''
          text = replace_once(text, old, new, "strengthen baseline thread config")

          old = '''                service_tier=None,
                session_config=baseline_config,
            )
'''
          new = '''                service_tier=None,
                session_config=baseline_config,
                app_server_command=baseline_app_server_command,
            )
'''
          text = replace_once(text, old, new, "pass baseline startup command")

          old = '''                disabled_skill_paths=baseline_disabled_skills,
                disabled_plugin_ids=[],
                exposed_core_skills={},
            )
'''
          new = '''                disabled_skill_paths=baseline_disabled_skills,
                disabled_plugin_ids=baseline_plugin_ids,
                startup_config_overrides=baseline_startup_overrides,
                exposed_core_skills={},
            )
'''
          text = replace_once(text, old, new, "record baseline startup isolation")

          old = '''            print("[2/2] Installing core and running unprompted tiny-edit candidate...")
            installed_root = guard.install_candidate()
            with base.AppServer(
                command=launchers.app_server_command,
'''
          new = '''            print("[2/2] Installing core and running unprompted tiny-edit candidate...")
            installed_root = guard.install_candidate()
            candidate_plugin_ids = installed_plugin_ids(launchers)
            (
                candidate_app_server_command,
                candidate_startup_overrides,
            ) = build_isolated_app_server_command(
                launchers=launchers,
                installed_plugin_ids=candidate_plugin_ids,
                plugins_enabled=True,
                enabled_plugin_id=base.PLUGIN_ID,
            )
            with base.AppServer(
                command=candidate_app_server_command,
'''
          text = replace_once(text, old, new, "build candidate startup isolation")

          old = '''            candidate_plugin_ids = installed_plugin_ids(launchers)
            (
                candidate_config,
'''
          new = '''            (
                candidate_config,
'''
          text = replace_once(text, old, new, "remove duplicate candidate inventory")

          old = '''                service_tier=baseline_turn.service_tier,
                session_config=candidate_config,
            )
'''
          new = '''                service_tier=baseline_turn.service_tier,
                session_config=candidate_config,
                app_server_command=candidate_app_server_command,
            )
'''
          text = replace_once(text, old, new, "pass candidate startup command")

          old = '''                disabled_skill_paths=candidate_disabled_skills,
                disabled_plugin_ids=candidate_disabled_plugins,
                exposed_core_skills=exposed_core,
            )
'''
          new = '''                disabled_skill_paths=candidate_disabled_skills,
                disabled_plugin_ids=candidate_disabled_plugins,
                startup_config_overrides=candidate_startup_overrides,
                exposed_core_skills=exposed_core,
            )
'''
          text = replace_once(text, old, new, "record candidate startup isolation")

          script_path.write_text(text, encoding="utf-8", newline="\n")

          test_path = Path("tests/test_codex_negative_smoke.py")
          tests = test_path.read_text(encoding="utf-8")
          tests = replace_once(
              tests,
              "import tempfile\nimport unittest\n",
              "import tempfile\nimport tomllib\nimport unittest\n",
              "import tomllib",
          )
          tests = replace_once(
              tests,
              '''            self.assertTrue(config["features"]["plugins"])
            self.assertEqual(disabled, [str(foreign)])
''',
              '''            self.assertTrue(config["features"]["plugins"])
            self.assertFalse(config["features"]["remote_plugin"])
            self.assertFalse(config["features"]["recommended_plugins"])
            self.assertFalse(config["features"]["plugin_sharing"])
            self.assertFalse(config["features"]["code_mode"])
            self.assertEqual(disabled, [str(foreign)])
''',
              "assert thread isolation features",
          )

          insert_marker = "\n    def test_skill_reference_detection_is_path_based(self) -> None:\n"
          startup_test = r'''

    def test_startup_command_disables_remote_catalog_and_foreign_plugins(self) -> None:
        launchers = base.CodexLaunchers(
            cli_prefix=("node", "codex.js"),
            app_server_command=("node", "codex.js", "app-server", "--listen", "stdio://"),
            node_executable="node",
            version_text="codex-cli 0.147.0",
            version=(0, 147, 0),
        )
        foreign_plugin = "fable-advisor@foreign-marketplace"
        command, overrides = module.build_isolated_app_server_command(
            launchers=launchers,
            installed_plugin_ids=[base.PLUGIN_ID, foreign_plugin],
            plugins_enabled=True,
            enabled_plugin_id=base.PLUGIN_ID,
        )
        self.assertEqual(command[:3], ("node", "codex.js", "app-server"))
        self.assertEqual(command[-2:], ("--listen", "stdio://"))
        self.assertIn("features.plugins=true", overrides)
        self.assertIn("features.remote_plugin=false", overrides)
        self.assertIn("features.apps=false", overrides)
        plugin_override = next(value for value in overrides if value.startswith("plugins="))
        plugin_table = tomllib.loads("_x_ = " + plugin_override)["_x_"]
        self.assertTrue(plugin_table[base.PLUGIN_ID]["enabled"])
        self.assertFalse(plugin_table[foreign_plugin]["enabled"])
        for override in overrides:
            self.assertIn(override, command)

    def test_baseline_startup_command_disables_all_plugins(self) -> None:
        launchers = base.CodexLaunchers(
            cli_prefix=("node", "codex.js"),
            app_server_command=("node", "codex.js", "app-server", "--listen", "stdio://"),
            node_executable="node",
            version_text="codex-cli 0.147.0",
            version=(0, 147, 0),
        )
        foreign_plugin = "fable-advisor@foreign-marketplace"
        _, overrides = module.build_isolated_app_server_command(
            launchers=launchers,
            installed_plugin_ids=[foreign_plugin],
            plugins_enabled=False,
            enabled_plugin_id=None,
        )
        self.assertIn("features.plugins=false", overrides)
        self.assertIn("features.remote_plugin=false", overrides)
        plugin_override = next(value for value in overrides if value.startswith("plugins="))
        plugin_table = tomllib.loads("_x_ = " + plugin_override)["_x_"]
        self.assertFalse(plugin_table[foreign_plugin]["enabled"])
'''
          tests = replace_once(tests, insert_marker, startup_test + insert_marker, "insert startup tests")
          tests = replace_once(
              tests,
              '''                disabled_skill_paths=[],
                disabled_plugin_ids=[],
                exposed_core_skills=exposed,
''',
              '''                disabled_skill_paths=[],
                disabled_plugin_ids=[],
                startup_config_overrides=["features.remote_plugin=false"],
                exposed_core_skills=exposed,
''',
              "update evaluation test call",
          )
          test_path.write_text(tests, encoding="utf-8", newline="\n")

          docs_path = Path("docs/live-smoke.md")
          docs = docs_path.read_text(encoding="utf-8")
          docs = replace_once(
              docs,
              "The baseline runs with plugins and ambient skills disabled. The candidate installs and exposes the core plugin without an explicit skill input. Foreign installed plugins and user skills, apps, memories, JavaScript REPL, and configured MCP servers remain disabled at the thread layer.",
              "The baseline runs with plugins and ambient skills disabled. The candidate installs and exposes the core plugin without an explicit skill input. Foreign installed plugins are disabled before each app-server process starts and again at the thread layer; the remote plugin catalog, foreign user skills, apps, memories, code mode, and configured MCP servers remain disabled.",
              "document process-level isolation",
          )
          docs = replace_once(
              docs,
              "- discovered foreign skill paths are disabled through per-thread config;\n",
              "- app-server startup overrides disable the remote plugin catalog and every foreign installed plugin before plugin capabilities are loaded;\n- discovered foreign skill paths are also disabled through per-thread config;\n",
              "document startup override boundary",
          )
          docs_path.write_text(docs, encoding="utf-8", newline="\n")

          changelog_path = Path("CHANGELOG.md")
          changelog = changelog_path.read_text(encoding="utf-8")
          anchor = "- Isolated negative-smoke candidates from every foreign installed plugin so plugin-contributed MCP servers cannot contaminate the campaign, and emit automatic diagnostics for every non-PASS outcome.\n"
          addition = anchor + "- Moved negative-smoke plugin isolation to app-server startup, disabled the remote plugin catalog for the campaign, and retained thread-level isolation as defense in depth.\n"
          changelog = replace_once(changelog, anchor, addition, "update changelog")
          changelog_path.write_text(changelog, encoding="utf-8", newline="\n")

          Path(".github/workflows/apply-negative-smoke-startup-isolation.yml").unlink()
          PY

      - name: Run focused negative smoke tests
        run: python -m unittest tests.test_codex_negative_smoke -v

      - name: Run complete repository bootstrap
        run: python scripts/bootstrap.py

      - name: Commit validated fix
        shell: bash
        run: |
          git config user.name "EgoistDeveloper"
          git config user.email "60717140+EgoistDeveloper@users.noreply.github.com"
          git add CHANGELOG.md docs/live-smoke.md scripts/run_codex_negative_smoke.py tests/test_codex_negative_smoke.py .github/workflows/apply-negative-smoke-startup-isolation.yml
          git commit -m "fix: isolate negative smoke at app-server startup"
          git push origin HEAD:fix/codex-negative-smoke-startup-isolation
