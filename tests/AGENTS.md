# Test instructions

- Tests must use the Python standard library only.
- Every routing rule needs a positive and a counterexample.
- Negative evidence fixtures must remain negative; do not weaken the gate to make them pass.
- Tests may create temporary directories but must not access the network or user-global configuration.
