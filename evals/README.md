# Behavior eval fixtures

`routing/` contains deterministic task profiles and expected topology decisions.

`evidence/` contains completion packets with expected pass/fail outcomes.

These fixtures test the machine-checkable layer only. They do not substitute for live model evals described in `docs/evaluation.md`.
